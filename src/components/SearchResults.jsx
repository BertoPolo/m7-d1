import { Row } from "react-bootstrap"

//fetch here and pass the stuff down there

const SearchResults = (company) => {
  return (
    <Row>
      {company.name}
      {company.name}
    </Row>
  )
}

export default SearchResults
